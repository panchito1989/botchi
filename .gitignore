# Dependencias y builds
node_modules/
.next/
out/

# Secretos — NUNCA subir (.env.local tiene la llave de Gemini)
.env
.env.local
.env*.local
*.pem
# device.json local con el device_token real (staging para scp a la Pi)
firmware/device.json
# Modelos de voz de Piper (binarios grandes, se descargan de HuggingFace)
*.onnx
*.onnx.json

# Python
__pycache__/
*.pyc

# SO / editor
.DS_Store
Thumbs.db
